import java.util.*

fun main(args: Array<String>) {


 val (x, y) = readLine()!!.split(" ").map { it.toInt() }

    for (i in 1..y) {
        if (i % x == 0) {
            println(i) 
        } else {
            print("$i ") 
        }
    }
	
}
