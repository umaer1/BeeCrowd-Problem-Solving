#include <stdio.h>
 
int main() {
 
    int num1 =0; 
    int num2 =1; 
    int n,sum, i;
 
        scanf("%d",&n);
 
    printf("%d %d",num1,num2);
 
 
    for(i=2; i<n; i++) {
        sum = num1 + num2;
 
        printf(" ");
        printf("%d",sum);
 
        num1 = num2;
        num2 = sum;
    }
 
 printf("\n");
    return 0;
}
